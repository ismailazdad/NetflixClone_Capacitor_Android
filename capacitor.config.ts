import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moviestrailer.app',
  appName: 'Movies Trailer',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
